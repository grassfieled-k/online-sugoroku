<%= bootstrap_form_with(model: job, local: true, layout: :horizontal) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :job_name %>
  </div>

  <div class="field">
    <%= form.number_field :job_type_id %>
  </div>

  <div class="field">
    <%= form.number_field :salary %>
  </div>

  <div class="field">
    <%= form.text_area :explanation %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
