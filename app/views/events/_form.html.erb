<%= bootstrap_form_with(model: event, local: true, layout: :horizontal) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :event_name %>
  </div>

  <div class="field">
    <%= form.text_field :message %>
  </div>

  <div class="field">
    <%= form.number_field :force_money %>
  </div>

  <div class="field">
    <%= form.number_field :force_intelligence %>
  </div>

  <div class="field">
    <%= form.number_field :force_physicality %>
  </div>

  <div class="field">
    <%= form.number_field :force_sense %>
  </div>

  <div class="field">
    <%= form.number_field :force_morality %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
